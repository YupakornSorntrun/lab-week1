

### ตาราง URI Schema (ของโปรเจกต์ `student-api` ทั้งหมด)

| Method | URI | คำอธิบาย | Status Code ที่เป็นไปได้ |
| --- | --- | --- | --- |
| GET | `/api/v1/students` | ดึงรายชื่อนักศึกษาทั้งหมด | 200 |
| GET | `/api/v1/students/{id}` | ดึงข้อมูลนักศึกษารายบุคคล (รองรับ `?include=courses`) | 200, 404 |
| POST | `/api/v1/students` | เพิ่มนักศึกษาใหม่ | 201, 400, 409 |
| PUT | `/api/v1/students/{id}` | แก้ไขข้อมูลนักศึกษาทั้งระเบียน | 200, 400, 404 |
| PATCH | `/api/v1/students/{id}` | แก้ไขข้อมูลนักศึกษาบางส่วน | 200, 404 |
| DELETE | `/api/v1/students/{id}` | ลบนักศึกษา | 200, 404 |


## Peer Check และแก้ไขตามข้อเสนอแนะ

### ขั้นตอนที่ 3.1 จับคู่ตรวจสอบงานกับเพื่อนร่วมชั้นเรียน
จับคู่กับเพื่อนในชั้นเรียน 1 คน (67160389) และแลกเปลี่ยนไฟล์ URI-SCHEMA.md ของกันและกัน จากนั้นตรวจสอบตามหัวข้อในแบบฟอร์มต่อไปนี้

| หัวข้อตรวจสอบ | ผ่าน / ไม่ผ่าน | ข้อเสนอแนะ |
| --- | --- | --- |
| URI ใช้คำนามพหูพจน์ ไม่มีคำกริยาปน | ผ่าน  | |
| ใช้ตัวพิมพ์เล็กและขีดกลาง ไม่ใช้ camelCase หรือ underscore | ผ่าน | |
| HTTP Method สอดคล้องกับการดำเนินการ (เช่น ไม่ใช้ GET เพื่อลบข้อมูล) | ไม่ผ่าน | POST แล้วไม่เพิ่มข้อมูลของนิสิตคนใหม่ ให้ทำตรงโค้ดในส่วนของ data students เพิ่ม nextID  |
| Status Code ครอบคลุมทั้งกรณีสำเร็จและกรณีผิดพลาด | ผ่าน | |
| ความสัมพันธ์ระหว่าง resource แสดงด้วย nested path อย่างเหมาะสม | ผ่าน | |

### ขั้นตอนที่ 3.2 ทดสอบ Endpoint ของเพื่อนด้วย Postman
นำ URI Schema ของเพื่อนมาทดสอบจริงผ่าน Postman อย่างน้อย 3 endpoint โดยเน้นกรณีขอบเขต (edge case) เช่น


* เรียกข้อมูลด้วย id ที่ไม่มีอยู่จริง ควรได้ 404
* ส่งข้อมูลไม่ครบในการสร้างข้อมูลใหม่ ควรได้ 400
* สร้างข้อมูลซ้ำ (ถ้ามีเงื่อนไข unique) ควรได้ 409